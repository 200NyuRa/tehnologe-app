.hamburger {
	clickedElement()
	box-sizing content-box
	width 30px
	height 21px
	display flex
	flex-direction column
	justify-content space-between
	cursor pointer

	&__line {
		display block
		height 3px
		background $extraColor
		transition transform $duration, opacity ($duration/2) ($duration/2)
	}
}